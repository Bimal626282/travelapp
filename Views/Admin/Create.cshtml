@model TravelApp.Models.TourPackage

@{
    ViewData["Title"] = "Add New Tour Package";
}

<div class="container mt-5">
    <h2 class="fw-bold text-primary">Create New Tour</h2>
    <form asp-controller="Admin" asp-action="Create" method="post">
        @* Form fields for Title, Price, etc. *@
        <div class="mb-3">
            <label asp-for="Title" class="form-label"></label>
            <input asp-for="Title" class="form-control" />
        </div>
        @* ... other fields ... *@
        <button type="submit" class="btn btn-primary">Save Package</button>
    </form>
</div>